const hostURL = {
    link:
      process.env.NODE_ENV === "production"
        ? "https://bussbookeee.onrender.com"
        : "http://localhost:4000",
  };
  
  export default hostURL;
  // const hostURL = {
  //   link:"http://localhost:4000"
  // };
  
  // export default hostURL;
  
